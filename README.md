# idrx-contract-demo

The authoritative API description for a **demo** of provider-embedded
integration assurance. `openapi.json` is the watched file: a control plane
reads it on a timer and structurally diffs it when it moves.

Synthetic. Not IDRX's real contract — IDRX publishes no OpenAPI spec.
